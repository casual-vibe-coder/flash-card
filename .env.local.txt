# ============================================================
#  Arabic Flashcard App — Environment Variables (.env)
# ------------------------------------------------------------
#  This file holds the app's secret keys.
#  Replace each "PASTE_...HERE" with the real value.
#  Your real values are most likely already saved in Vercel:
#    Vercel → your project → Settings → Environment Variables
#  KEEP THIS FILE PRIVATE. Never post it publicly or commit it.
# ============================================================

# --- REQUIRED ---------------------------------------------------------------

# Main AI — generates the flashcards / text.
# Get it at: https://openrouter.ai  →  Keys   (starts with sk-or-v1-...)
OPENROUTER_API_KEY=PASTE_OPENROUTER_KEY_HERE


# --- OPTIONAL (only needed for the matching feature) ------------------------

# DALL-E images. Get it at: https://platform.openai.com → API Keys (starts with sk-...)
OPENAI_API_KEY=PASTE_OPENAI_KEY_HERE

# Arabic text-to-speech (spoken audio). Google Cloud → Text-to-Speech API key.
GOOGLE_TTS_API_KEY=PASTE_GOOGLE_TTS_KEY_HERE

# Other Google services. Google Cloud Console → Credentials → API key.
GOOGLE_API_KEY=PASTE_GOOGLE_API_KEY_HERE

# Speech-to-text (listening to you speak). Get it at: https://deepgram.com → API Keys
DEEPGRAM_API_KEY=PASTE_DEEPGRAM_KEY_HERE

# Saving user data on the server. Firebase → Project Settings → Service Accounts →
# "Generate new private key". Paste the whole JSON on one line, wrapped in single quotes.
FIREBASE_SERVICE_ACCOUNT='PASTE_FIREBASE_SERVICE_ACCOUNT_JSON_HERE'
